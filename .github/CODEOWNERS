# Both maintainers own everything.
#
# GitHub excludes a PR's author from its own review request, so listing both
# here means every PR automatically requests the other person. With a
# two-person team, that review is the only thing standing between a bad idea
# and main — so there is no path that skips it.

*                       @Junhui20 @OSSWT

# Schema is the single source of truth for both languages. A change here
# ripples into generated Pydantic models and every stored row.
/packages/schema/       @Junhui20 @OSSWT

# Gitignored in practice, but guard the path so nothing private slips in.
/docs/                  @Junhui20 @OSSWT
