# Copy to .env.local. Both are public values — the anon key ships in the browser
# and grants nothing on its own; row-level security decides what a caller sees.
VITE_SUPABASE_URL=https://xxxxxxxxxxxx.supabase.co
VITE_SUPABASE_ANON_KEY=
